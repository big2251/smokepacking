<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Puff Premium | Breeze & Vozol</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; }
        .cart-modal { display: none; background: rgba(0,0,0,0.7); backdrop-filter: blur(5px); }
        .cart-modal.active { display: flex; }
        .product-card:hover img { transform: scale(1.1); }
    </style>
</head>
<body class="bg-gray-100">

    <header class="bg-black text-white sticky top-0 z-50 shadow-2xl border-b border-yellow-500/30">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <h1 class="text-2xl font-black tracking-tighter text-yellow-500 underline decoration-white">PUFF<span class="text-white italic">KING</span></h1>
            <button onclick="toggleCart()" class="bg-yellow-500 hover:bg-yellow-400 text-black font-bold px-6 py-2 rounded-full transition-all flex items-center gap-2">
                <span>🛒</span> <span id="cart-count">0</span> Panier
            </button>
        </div>
    </header>

    <div class="w-full h-40 bg-gradient-to-r from-gray-900 to-black flex items-center justify-center text-center p-4">
        <div>
            <h2 class="text-white text-2xl md:text-3xl font-bold uppercase tracking-widest">Breeze & Vozol Original</h2>
            <p class="text-yellow-500 text-sm">Livraison rapide en Francs CFA</p>
        </div>
    </div>

    <div class="container mx-auto px-4 mt-6 flex gap-3 overflow-x-auto pb-4">
        <button onclick="filterBrand('all')" class="bg-black text-white px-6 py-2 rounded-xl text-sm font-bold flex-shrink-0">Tout</button>
        <button onclick="filterBrand('Breeze')" class="bg-blue-600 text-white px-6 py-2 rounded-xl text-sm font-bold flex-shrink-0">Breeze</button>
        <button onclick="filterBrand('Vozol')" class="bg-green-600 text-white px-6 py-2 rounded-xl text-sm font-bold flex-shrink-0">Vozol</button>
    </div>

    <main class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8" id="product-list">
            </div>
    </main>

    <div id="cart-modal" class="cart-modal fixed inset-0 z-[100] p-4 flex justify-center items-center">
        <div class="bg-white rounded-3xl w-full max-w-md shadow-2xl overflow-hidden animate-in fade-in zoom-in duration-300">
            <div class="p-6 bg-gray-50 border-b flex justify-between items-center">
                <h3 class="font-bold text-xl uppercase">Ma Commande</h3>
                <button onclick="toggleCart()" class="text-gray-400 text-2xl">&times;</button>
            </div>
            <div id="cart-items" class="p-6 max-h-[350px] overflow-y-auto space-y-4 text-gray-700">
                </div>
            <div class="p-6 border-t bg-gray-50">
                <div class="flex justify-between font-black text-2xl mb-6 text-black">
                    <span>Total :</span>
                    <span id="cart-total">0</span> <span class="text-sm self-center ml-1">FCFA</span>
                </div>
                <button onclick="checkout()" class="w-full bg-green-600 hover:bg-green-500 text-white font-black py-4 rounded-2xl shadow-lg transition-transform active:scale-95 flex justify-center items-center gap-2">
                    ✅ Commander sur WhatsApp
                </button>
            </div>
        </div>
    </div>

    <script>
        const products = [
            // BREEZE MODELS
            { id: 1, name: "Breeze Pro", taffs: 2000, price: 7500, brand: "Breeze", img: "https://images.tcdn.com.br/img/img_prod/1126742/pod_descartavel_breeze_pro_2000_puffs_1907_1_f854a61327178051e944d18ecf52277d.jpg" },
            { id: 2, name: "Breeze Elite", taffs: 4000, price: 10000, brand: "Breeze", img: "https://vape-brands.com/wp-content/uploads/2023/10/Breeze-Elite-Strawberry-Peach-Mint.jpg" },
            { id: 3, name: "Breeze Prime", taffs: 6000, price: 14000, brand: "Breeze", img: "https://breezesmoke.com/cdn/shop/files/Breeze_Prime_Product_Images_Strawberry_Apple_Watermelon_300x.png?v=1692131234" },
            
            // VOZOL MODELS
            { id: 4, name: "Vozol Star 600", taffs: 600, price: 3500, brand: "Vozol", img: "https://cdn.shopify.com/s/files/1/0611/4023/8505/products/vozol-star-600-disposable-vape-pod-kit-p2901-7008_800x.jpg" },
            { id: 5, name: "Vozol Star 12k", taffs: 12000, price: 18000, brand: "Vozol", img: "https://vozol-vapes.com/wp-content/uploads/2023/12/VOZOL-Star-12000-Strawberry-Ice-Cream.jpg" },
            { id: 6, name: "Vozol Vista 20k", taffs: 20000, price: 22000, brand: "Vozol", img: "https://vozol-vapes.com/wp-content/uploads/2024/04/VOZOL-Vista-20000-Cherry-Cola.jpg" },
            { id: 7, name: "Vozol Gear Hookah", taffs: 40000, price: 28000, brand: "Vozol", img: "https://m.media-amazon.com/images/I/61M07Kj6C3L.jpg" }
        ];

        let cart = [];

        function renderProducts(filter = 'all') {
            const list = document.getElementById('product-list');
            const filtered = filter === 'all' ? products : products.filter(p => p.brand === filter);
            
            list.innerHTML = filtered.map(p => `
                <div class="product-card bg-white rounded-3xl overflow-hidden shadow-lg border border-gray-100 group transition-all">
                    <div class="relative h-64 overflow-hidden bg-gray-200">
                        <img src="${p.img}" class="w-full h-full object-cover transition-transform duration-500">
                        <div class="absolute top-3 left-3 bg-black/70 text-white text-[10px] px-3 py-1 rounded-full font-bold">
                            ${p.brand.toUpperCase()}
                        </div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-extrabold text-gray-800">${p.name}</h3>
                        <p class="text-sm text-yellow-600 font-semibold mb-4">${p.taffs.toLocaleString()} Taffs (Bouffées)</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-black text-black">${p.price.toLocaleString()} <small class="text-xs">CFA</small></span>
                            <button onclick="addToCart(${p.id})" class="bg-black text-white w-12 h-12 rounded-2xl flex items-center justify-center hover:bg-yellow-500 hover:text-black transition-colors font-bold text-xl shadow-md">
                                +
                            </button>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function filterBrand(brand) {
            renderProducts(brand);
        }

        function addToCart(id) {
            const product = products.find(p => p.id === id);
            cart.push(product);
            updateCart();
        }

        function updateCart() {
            document.getElementById('cart-count').innerText = cart.length;
            const cartItems = document.getElementById('cart-items');
            const total = cart.reduce((sum, item) => sum + item.price, 0);
            
            if(cart.length === 0) {
                cartItems.innerHTML = `<p class="text-center text-gray-400">Votre panier est vide</p>`;
            } else {
                cartItems.innerHTML = cart.map((item, index) => `
                    <div class="flex justify-between items-center bg-gray-100 p-4 rounded-2xl">
                        <div>
                            <p class="font-bold text-sm text-black">${item.name}</p>
                            <p class="text-xs text-gray-500">${item.price.toLocaleString()} FCFA</p>
                        </div>
                        <button onclick="removeItem(${index})" class="text-red-500 bg-white w-8 h-8 rounded-full shadow-sm">✕</button>
                    </div>
                `).join('');
            }
            
            document.getElementById('cart-total').innerText = total.toLocaleString();
        }

        function removeItem(index) {
            cart.splice(index, 1);
            updateCart();
        }

        function toggleCart() {
            document.getElementById('cart-modal').classList.toggle('active');
        }

        function checkout() {
            if(cart.length === 0) return alert("Ajoutez des puffs au panier !");
            let msg = "👋 *COMMANDE PUFF LUXE*\n\n";
            cart.forEach(item => msg += `💨 *${item.name}* (${item.taffs} taffs) - ${item.price} FCFA\n`);
            const total = cart.reduce((sum, item) => sum + item.price, 0);
            msg += `\n💵 *TOTAL À PAYER : ${total} FCFA*`;
            
            // REMPLACE PAR TON NUMÉRO (ex: 2250700000000)
            const tonNumero = "2250000000000"; 
            window.open(`https://wa.me/${tonNumero}?text=${encodeURIComponent(msg)}`);
        }

        renderProducts();
    </script>
</body>
</html>
